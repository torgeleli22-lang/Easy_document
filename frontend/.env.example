# API 엔드포인트 주소. 공개돼도 되는 값이라 저장소에 커밋해도 괜찮습니다.
# 실제 비밀키는 절대 프런트엔드에 넣지 않습니다 (기획서 4.2.2).

# 스트리밍이 필요 없는 짧은 요청용 (presigned URL 발급 등)
VITE_REST_API_URL=

# 실시간 분석용 WebSocket 엔드포인트
VITE_WS_URL=

# 비어 있으면 목업 데이터로 동작합니다. 백엔드가 준비되면 false로 바꿉니다.
VITE_USE_MOCK=true
